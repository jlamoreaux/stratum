# Assets-only Worker serving the built docs site (dist/).
name = "stratum-docs"
compatibility_date = "2026-08-01"

routes = [
  { pattern = "docs.usestratum.dev", custom_domain = true }
]

[assets]
directory = "./dist"
not_found_handling = "404-page"
